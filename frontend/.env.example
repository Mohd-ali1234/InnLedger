# Base URL the frontend calls. Leave as /api to use the Vite dev proxy (recommended).
VITE_API_BASE_URL=/api

# Where the dev proxy forwards /api requests. Change if your backend runs elsewhere.
VITE_PROXY_TARGET=http://127.0.0.1:8000
